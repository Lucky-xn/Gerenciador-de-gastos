import express from 'express';

// import authRoutes from './system/authRoutes';
// import userRoutes from './system/userRoutes';

const router = express.Router();

export default (): express.Router => {
  // authRoutes(router);
  // userRoutes(router);

  return router
}

